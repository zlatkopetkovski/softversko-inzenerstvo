@model IEnumerable<PosetiMe.Models.tblVisit>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblLocal.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblUser.ID)
        </th>
        <th></th>
    </tr>
 
@foreach (var item in Model)
{
    if (item.ID_User == ViewBag.user) // проверва дали посетата на локалот е од најавениот корисник
    {
    <tr>
        <td>
            @Html.DisplayFor(Model => item.Date)
        </td>
        <td>
            @Html.DisplayFor(Model => item.tblLocal.Name)
        </td>
        <td>
            @Html.DisplayFor(Model => item.tblUser.ID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
            @Html.ActionLink("Details", "Details", new { id = item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ID })
        </td>
    </tr>}
}

</table>
