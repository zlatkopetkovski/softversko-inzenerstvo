@model IEnumerable<PosetiMe.Models.tblComment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Коментари
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblLocal.Name)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.tblUser.ID)
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model)
{
    //if (item.ID_User == ViewBag.user && item.ID_Local == ViewBag.local)
    //{ 
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Comment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblLocal.Name)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.tblUser.ID)
        </td>*@
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
            @Html.ActionLink("Details", "Details", new { id = item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ID })
        </td>
    </tr>
    @*}*@
}

</table>
